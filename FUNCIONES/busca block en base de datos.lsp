(defun SBLBD (NoBLOCK)
  (SETQ PR NIL)
  (SETQ PROB (TBLNEXT "BLOCK" T))
  (WHILE (and PROB (= pr nil))
    (SETQ NPROV (CDR (CAR (CDR PROB))))
    (IF (= NPROV NoBLOCK) (SETQ PR "S"))
    (SETQ PROB (TBLNEXT "BLOCK"))
  )
  (SETq RESP PR)
)